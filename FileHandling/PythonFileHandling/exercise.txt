What is a function used fot opening files?
    open()

By default the file is oppened in text mode, but you can also
open it in binary mode.
Which one of the following syntaxes, opens the file in binary mode?
    x = open('demofile.txt', 'b)

True or False.
The default opening mode when opening a file with the open()
function is 'r' for 'reading'
    True