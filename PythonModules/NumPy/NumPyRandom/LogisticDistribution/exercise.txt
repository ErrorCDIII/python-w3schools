The random.logistic() method has three parameters, which ones?

    loc scale size

Insert the missing part to generate a logistic distribution

    x = random.logistic(size=(2, 3))

Insert the missing part to generate a losgistic distribution with mean at 1
and standard deviation is 2

    x = random.logistic(loc=1, scale=2, size=(2, 3))
