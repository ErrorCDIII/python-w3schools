One of these functions can be used to join two or more arrays.
Which one?
    concatenate()

Use a correct NumPy method to join two arrays into a single arrays
    arr1 = np.array([1, 2, 3])

    arr2 = np.array([4, 5, 6])

    arr = np.concatenate((arr1, arr2))

Which NumPy function can be used to join two arrays, stacking along
collumns?

    vstack()